<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://kaurmahima.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://kaurmahima.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-27T01:55:51+00:00</updated><id>https://kaurmahima.github.io/feed.xml</id><title type="html">Mahima Kaur</title><subtitle>Mahima Kaur is an Analytics Data Engineer building reproducible data pipelines, analytics models, dashboards, and reliable data products. </subtitle><entry><title type="html">Building Analytics Marts with dbt and DuckDB</title><link href="https://kaurmahima.github.io/blog/2026/building-analytics-marts-with-dbt-and-duckdb/" rel="alternate" type="text/html" title="Building Analytics Marts with dbt and DuckDB"/><published>2026-05-24T00:00:00+00:00</published><updated>2026-05-24T00:00:00+00:00</updated><id>https://kaurmahima.github.io/blog/2026/building-analytics-marts-with-dbt-and-duckdb</id><content type="html" xml:base="https://kaurmahima.github.io/blog/2026/building-analytics-marts-with-dbt-and-duckdb/"><![CDATA[<p>A dashboard or recurring report is only as stable as the analytical tables behind it. When metric logic is embedded directly in application code or scattered across notebooks, changes become difficult to review and results become difficult to reproduce.</p> <p>A practical alternative is to model analytical tables in <strong>dbt</strong> and execute them in <strong>DuckDB</strong>. dbt provides structure, dependencies, documentation, and testing; DuckDB provides a fast analytical engine that can read local Parquet inputs and materialize warehouse tables without operating a database server.</p> <h2 id="separate-staging-from-business-logic">Separate Staging From Business Logic</h2> <p>The first layer should make source data consistent, not answer every analytical question. A staging model typically handles:</p> <ul> <li>Renaming columns to stable conventions.</li> <li>Casting strings into dates, numeric values, and categories.</li> <li>Standardizing missing or malformed values.</li> <li>Defining a reliable row grain.</li> </ul> <p>Once staging is stable, marts can focus on measures such as daily volume, member mix, average duration, cost, or event rates.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Parquet inputs
    |
    v
stg_events + stg_context
    |
    v
fct_events -&gt; fct_events_daily -&gt; mart_dashboard_metrics
</code></pre></div></div> <h2 id="build-marts-around-questions">Build Marts Around Questions</h2> <p>An analytical mart should have a clear consumer and grain. For example, a daily activity mart could contain one row per date and expose the measures used in a reporting interface:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span>
  <span class="n">event_date</span><span class="p">,</span>
  <span class="k">count</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">as</span> <span class="n">total_events</span><span class="p">,</span>
  <span class="n">count_if</span><span class="p">(</span><span class="n">user_type</span> <span class="o">=</span> <span class="s1">'member'</span><span class="p">)</span> <span class="k">as</span> <span class="n">member_events</span><span class="p">,</span>
  <span class="k">avg</span><span class="p">(</span><span class="n">duration_minutes</span><span class="p">)</span> <span class="k">as</span> <span class="n">avg_duration_minutes</span>
<span class="k">from</span> <span class="p">{{</span> <span class="k">ref</span><span class="p">(</span><span class="s1">'stg_events'</span><span class="p">)</span> <span class="p">}}</span>
<span class="k">group</span> <span class="k">by</span> <span class="n">event_date</span>
</code></pre></div></div> <p>A separate enrichment model can join contextual data such as weather, holidays, campaigns, regions, or other external features. Separating base aggregation from enrichment makes it easier to inspect where a metric changed.</p> <h2 id="add-tests-at-the-grain-of-the-model">Add Tests At The Grain Of The Model</h2> <p>Testing is valuable when it asserts assumptions that downstream users depend on. Typical dbt checks include:</p> <ul> <li>Unique and non-null keys for fact and dimension models.</li> <li>Accepted categorical values.</li> <li>Referential integrity between fact and dimension tables.</li> <li>Custom tests for measures that cannot be negative.</li> </ul> <p>For a daily mart, the date should generally be unique and not null. For a trip- or visit-level fact model, the event identifier should be unique. These tests turn model contracts into executable checks.</p> <h2 id="materialize-intentionally">Materialize Intentionally</h2> <p>DuckDB makes local transformations inexpensive, but materialization choices still matter:</p> <ul> <li>Use <strong>views</strong> for lightweight staging models and rapidly changing logic.</li> <li>Use <strong>tables</strong> for marts read repeatedly by dashboards or analysis queries.</li> <li>Use incremental or partition-based rebuilding when source history grows.</li> </ul> <p>This avoids unnecessary compute while keeping presentation layers responsive.</p> <h2 id="a-simple-build-workflow">A Simple Build Workflow</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>dbt
dbt build
duckdb ../warehouse/analytics.duckdb
</code></pre></div></div> <p>Within DuckDB, validation can continue at the model output:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span> <span class="o">*</span>
<span class="k">from</span> <span class="n">mart_dashboard_metrics</span>
<span class="k">order</span> <span class="k">by</span> <span class="n">event_date</span> <span class="k">desc</span>
<span class="k">limit</span> <span class="mi">10</span><span class="p">;</span>
</code></pre></div></div> <h2 id="takeaway">Takeaway</h2> <p>dbt and DuckDB are a strong combination for analytics engineering projects that need transparent SQL logic, fast local execution, and reliable marts for dashboards or recurring reporting. The essential practice is to make each model answer one clear question at a documented grain, with tests that protect downstream trust.</p>]]></content><author><name>Mahima Kaur</name></author><category term="analytics-engineering"/><category term="data-engineering"/><category term="data-quality"/><summary type="html"><![CDATA[How dbt and DuckDB work together to create transparent, testable analytical models from Parquet data.]]></summary></entry><entry><title type="html">Designing Analytical Data Pipelines with Parquet and DuckDB</title><link href="https://kaurmahima.github.io/blog/2026/designing-health-data-pipelines-with-parquet-and-duckdb/" rel="alternate" type="text/html" title="Designing Analytical Data Pipelines with Parquet and DuckDB"/><published>2026-05-22T00:00:00+00:00</published><updated>2026-05-22T00:00:00+00:00</updated><id>https://kaurmahima.github.io/blog/2026/designing-health-data-pipelines-with-parquet-and-duckdb</id><content type="html" xml:base="https://kaurmahima.github.io/blog/2026/designing-health-data-pipelines-with-parquet-and-duckdb/"><![CDATA[<p>Analytical datasets often arrive as many files split by year, source, region, or population. Before an analyst can answer a business or research question, the data engineering work has to solve a more basic problem: how can these inputs be transformed into datasets that are consistent, traceable, and efficient to query?</p> <p>A useful local-first architecture is to store standardized extracts as <strong>partitioned Parquet</strong> and query or transform them with <strong>DuckDB</strong>. This approach provides columnar storage and SQL analytics without requiring a long-running database service.</p> <h2 id="why-parquet-for-the-storage-layer">Why Parquet For The Storage Layer</h2> <p>CSV files are useful as source inputs, but they are not an ideal analytical storage format. They require repeated parsing, do not preserve data types reliably, and often force queries to scan columns that are not needed.</p> <p>Parquet improves that workflow by providing:</p> <ul> <li>Columnar storage, so analytical queries read only relevant fields.</li> <li>Preserved types, which reduces repeated cleaning logic.</li> <li>Compression, which helps when datasets span many files or years.</li> <li>Partition-friendly layouts for incremental processing.</li> </ul> <p>A year- and month-partitioned layer can look like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>data/bronze/
  year=2024/month=01/records.parquet
  year=2024/month=02/records.parquet
  year=2024/month=03/records.parquet
</code></pre></div></div> <p>That layout makes data lineage understandable and supports targeted reprocessing when a single period needs correction.</p> <h2 id="using-duckdb-as-an-analytical-engine">Using DuckDB As An Analytical Engine</h2> <p>DuckDB is effective for this pattern because it queries Parquet directly and supports familiar SQL transformation workflows. For example, an analyst can aggregate a partitioned dataset without first loading it into a server database:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span>
  <span class="n">service_year</span><span class="p">,</span>
  <span class="k">count</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">as</span> <span class="n">records</span><span class="p">,</span>
  <span class="k">count</span><span class="p">(</span><span class="k">distinct</span> <span class="n">patient_id</span><span class="p">)</span> <span class="k">as</span> <span class="n">patients</span>
<span class="k">from</span> <span class="n">read_parquet</span><span class="p">(</span><span class="s1">'data/bronze/**/*.parquet'</span><span class="p">,</span> <span class="n">hive_partitioning</span> <span class="o">=</span> <span class="k">true</span><span class="p">)</span>
<span class="k">group</span> <span class="k">by</span> <span class="n">service_year</span>
<span class="k">order</span> <span class="k">by</span> <span class="n">service_year</span><span class="p">;</span>
</code></pre></div></div> <p>This is useful during pipeline development because validation queries can run against the stored output immediately.</p> <h2 id="bronze-staging-and-mart-layers">Bronze, Staging, And Mart Layers</h2> <p>Separating transformations into layers prevents cleaning rules from being mixed with analytical definitions:</p> <ol> <li><strong>Bronze</strong> stores standardized source extracts with minimal business logic.</li> <li><strong>Staging</strong> applies naming conventions, type casting, deduplication, and normalized codes.</li> <li><strong>Marts</strong> define analysis-ready outcomes, cohorts, or aggregate measures.</li> </ol> <p>For any governed analytical dataset, this separation matters. A transformation such as normalizing source categories or harmonizing identifiers should be documented independently from a metric such as activity, utilization, revenue, or retention.</p> <h2 id="validation-at-each-boundary">Validation At Each Boundary</h2> <p>A data pipeline is not complete when a file is written. Each transition should check what changed:</p> <ul> <li>Record counts before and after transformation.</li> <li>Null rates for critical identifiers and dates.</li> <li>Duplicates at the intended grain.</li> <li>Valid ranges for dates, amounts, durations, and category codes.</li> <li>Partition completeness for expected time periods.</li> </ul> <p>An example validation query for a visit-level table:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span>
  <span class="k">count</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">as</span> <span class="n">visits</span><span class="p">,</span>
  <span class="k">count</span><span class="p">(</span><span class="k">distinct</span> <span class="n">visit_id</span><span class="p">)</span> <span class="k">as</span> <span class="n">distinct_visits</span><span class="p">,</span>
  <span class="k">sum</span><span class="p">(</span><span class="k">case</span> <span class="k">when</span> <span class="n">patient_id</span> <span class="k">is</span> <span class="k">null</span> <span class="k">then</span> <span class="mi">1</span> <span class="k">else</span> <span class="mi">0</span> <span class="k">end</span><span class="p">)</span> <span class="k">as</span> <span class="n">missing_patient_ids</span><span class="p">,</span>
  <span class="k">sum</span><span class="p">(</span><span class="k">case</span> <span class="k">when</span> <span class="n">discharge_date</span> <span class="o">&lt;</span> <span class="n">admission_date</span> <span class="k">then</span> <span class="mi">1</span> <span class="k">else</span> <span class="mi">0</span> <span class="k">end</span><span class="p">)</span> <span class="k">as</span> <span class="n">invalid_stays</span>
<span class="k">from</span> <span class="n">analytics</span><span class="p">.</span><span class="n">visits</span><span class="p">;</span>
</code></pre></div></div> <h2 id="practical-takeaway">Practical Takeaway</h2> <p>Partitioned Parquet plus DuckDB is a strong foundation for analytical pipelines when teams need reproducible transformations, efficient local iteration, and clear data products. The technology is lightweight; the engineering standard should still be rigorous: explicit layers, documented grain, and validation that makes analytical results defensible.</p>]]></content><author><name>Mahima Kaur</name></author><category term="data-engineering"/><category term="analytics-engineering"/><category term="reproducibility"/><summary type="html"><![CDATA[Practical design decisions for turning large, multi-file datasets into queryable analytical layers.]]></summary></entry><entry><title type="html">Serving Analytical Data Products with Streamlit</title><link href="https://kaurmahima.github.io/blog/2026/serving-analytical-data-with-streamlit/" rel="alternate" type="text/html" title="Serving Analytical Data Products with Streamlit"/><published>2026-05-18T00:00:00+00:00</published><updated>2026-05-18T00:00:00+00:00</updated><id>https://kaurmahima.github.io/blog/2026/serving-analytical-data-with-streamlit</id><content type="html" xml:base="https://kaurmahima.github.io/blog/2026/serving-analytical-data-with-streamlit/"><![CDATA[<p>An analytical dashboard is most maintainable when it is a consumer of trusted data products, not the place where business logic is recreated. <strong>Streamlit</strong> makes it straightforward to publish interactive analyses in Python, but the reliability of the dashboard depends on how the data layer is structured.</p> <h2 id="keep-modeling-upstream">Keep Modeling Upstream</h2> <p>A common failure mode is to load raw files in the dashboard and calculate every metric in Python each time the app runs. That creates several problems:</p> <ul> <li>Metric definitions are harder to test separately from interface behavior.</li> <li>Loading and transformation costs slow interaction.</li> <li>Analysts cannot easily query the same modeled output outside the dashboard.</li> <li>Business logic becomes coupled to chart code.</li> </ul> <p>A stronger architecture puts transformations in SQL models or pipeline steps and exposes analysis-ready tables to the application:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source files -&gt; Parquet -&gt; dbt/DuckDB marts -&gt; Streamlit
</code></pre></div></div> <h2 id="design-dashboard-tables-around-user-actions">Design Dashboard Tables Around User Actions</h2> <p>The serving model should match the kinds of interactions users need. Common dashboard-ready tables include:</p> <ul> <li>Daily KPI tables for summary cards and trend charts.</li> <li>Entity-level aggregates for map or ranking views.</li> <li>Flow tables for origin-destination exploration.</li> <li>Hourly aggregates for time-of-day patterns.</li> </ul> <p>For example, a daily KPI query should be compact:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span>
  <span class="n">activity_date</span><span class="p">,</span>
  <span class="n">total_events</span><span class="p">,</span>
  <span class="n">active_entities</span><span class="p">,</span>
  <span class="n">avg_duration_minutes</span>
<span class="k">from</span> <span class="n">mart_daily_kpis</span>
<span class="k">where</span> <span class="n">activity_date</span> <span class="k">between</span> <span class="o">?</span> <span class="k">and</span> <span class="o">?</span>
<span class="k">order</span> <span class="k">by</span> <span class="n">activity_date</span><span class="p">;</span>
</code></pre></div></div> <p>The app filters and visualizes a defined model; it does not redefine the calculation.</p> <h2 id="query-duckdb-safely-from-streamlit">Query DuckDB Safely From Streamlit</h2> <p>DuckDB works well behind local analytical dashboards because it can serve modeled tables or directly query Parquet when appropriate:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">duckdb</span>
<span class="kn">import</span> <span class="n">streamlit</span> <span class="k">as</span> <span class="n">st</span>

<span class="nd">@st.cache_data</span>
<span class="k">def</span> <span class="nf">load_daily_kpis</span><span class="p">(</span><span class="n">start_date</span><span class="p">,</span> <span class="n">end_date</span><span class="p">):</span>
    <span class="k">with</span> <span class="n">duckdb</span><span class="p">.</span><span class="nf">connect</span><span class="p">(</span><span class="sh">"</span><span class="s">warehouse/analytics.duckdb</span><span class="sh">"</span><span class="p">,</span> <span class="n">read_only</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span> <span class="k">as</span> <span class="n">conn</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">conn</span><span class="p">.</span><span class="nf">execute</span><span class="p">(</span>
            <span class="sh">"""</span><span class="s">
            select *
            from mart_daily_kpis
            where activity_date between ? and ?
            order by activity_date
            </span><span class="sh">"""</span><span class="p">,</span>
            <span class="p">[</span><span class="n">start_date</span><span class="p">,</span> <span class="n">end_date</span><span class="p">],</span>
        <span class="p">).</span><span class="nf">df</span><span class="p">()</span>
</code></pre></div></div> <p>Using a read-only connection and cached results separates application reads from pipeline writes and improves dashboard responsiveness.</p> <h2 id="display-metric-definitions">Display Metric Definitions</h2> <p>Dashboards become easier to trust when the user can understand each metric. Include concise definitions for:</p> <ul> <li>The table grain.</li> <li>Date coverage.</li> <li>Filters applied.</li> <li>Whether a measure represents events, unique entities, averages, or rates.</li> <li>Refresh time or pipeline-run context.</li> </ul> <p>This is especially important when similar measures can be interpreted differently, such as events versus users or totals versus normalized rates.</p> <h2 id="handle-empty-and-partial-data">Handle Empty And Partial Data</h2> <p>Applications need graceful behavior when:</p> <ul> <li>A date range has no records.</li> <li>A partition has not completed processing.</li> <li>A required model is unavailable.</li> <li>Filters reduce data below meaningful levels.</li> </ul> <p>Instead of rendering misleading zeros, the interface should tell the user when data is unavailable or still processing.</p> <h2 id="takeaway">Takeaway</h2> <p>Streamlit is effective for analytical delivery when paired with modeled, tested, queryable data products. A dashboard should make insight accessible; it should not hide transformation logic that belongs in the data pipeline.</p>]]></content><author><name>Mahima Kaur</name></author><category term="analytics-engineering"/><category term="data-engineering"/><category term="data-quality"/><summary type="html"><![CDATA[Designing dashboards that consume modeled metrics rather than repeating transformation logic in the app layer.]]></summary></entry><entry><title type="html">Validating analytics-ready data before analysis</title><link href="https://kaurmahima.github.io/blog/2026/validating-analytics-ready-health-data/" rel="alternate" type="text/html" title="Validating analytics-ready data before analysis"/><published>2026-05-15T00:00:00+00:00</published><updated>2026-05-15T00:00:00+00:00</updated><id>https://kaurmahima.github.io/blog/2026/validating-analytics-ready-health-data</id><content type="html" xml:base="https://kaurmahima.github.io/blog/2026/validating-analytics-ready-health-data/"><![CDATA[<p>An analytics-ready dataset should be more than a cleaned table. It should have a documented grain, predictable identifiers, valid time relationships, and measurements that can be trusted in downstream analysis.</p> <p>In analytical workflows, weak validation can quietly affect customer counts, utilization trends, costs, or modeled outcomes. The checks below are a practical baseline before exposing a dataset to analysts or dashboards.</p> <h2 id="1-define-the-grain-first">1. Define The Grain First</h2> <p>Before validating a table, state what one row represents:</p> <ul> <li>One beneficiary.</li> <li>One enrollment period.</li> <li>One admission.</li> <li>One claim line.</li> <li>One patient-month summary.</li> </ul> <p>Without a clear grain, duplicate checks and joins cannot be interpreted correctly. A duplicated beneficiary row may be a defect in a beneficiary table but completely expected in an admissions table.</p> <h2 id="2-validate-identifiers">2. Validate Identifiers</h2> <p>Identifiers determine whether records can be joined and whether events can be counted correctly. At minimum, check:</p> <ul> <li>Missing primary identifiers.</li> <li>Duplicate keys at the stated grain.</li> <li>Orphan records after joining source tables.</li> <li>Unexpected changes in distinct identifier counts between pipeline runs.</li> </ul> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span>
  <span class="k">count</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">as</span> <span class="k">rows</span><span class="p">,</span>
  <span class="k">count</span><span class="p">(</span><span class="k">distinct</span> <span class="n">admission_id</span><span class="p">)</span> <span class="k">as</span> <span class="n">admissions</span><span class="p">,</span>
  <span class="n">count_if</span><span class="p">(</span><span class="n">admission_id</span> <span class="k">is</span> <span class="k">null</span><span class="p">)</span> <span class="k">as</span> <span class="n">missing_admission_ids</span><span class="p">,</span>
  <span class="n">count_if</span><span class="p">(</span><span class="n">patient_id</span> <span class="k">is</span> <span class="k">null</span><span class="p">)</span> <span class="k">as</span> <span class="n">missing_patient_ids</span>
<span class="k">from</span> <span class="n">mart_admissions</span><span class="p">;</span>
</code></pre></div></div> <h2 id="3-test-time-relationships">3. Test Time Relationships</h2> <p>Date logic is central to utilization and outcomes analysis. Common checks include:</p> <ul> <li>Admission date is not after discharge date.</li> <li>Enrollment spans include the dates of eligible events.</li> <li>Year and month partitions agree with event dates.</li> <li>Follow-up windows are available before outcome calculation.</li> </ul> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span> <span class="k">count</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">as</span> <span class="n">invalid_length_of_stay</span>
<span class="k">from</span> <span class="n">mart_admissions</span>
<span class="k">where</span> <span class="n">discharge_date</span> <span class="o">&lt;</span> <span class="n">admission_date</span><span class="p">;</span>
</code></pre></div></div> <h2 id="4-monitor-clinical-and-financial-measures">4. Monitor Clinical And Financial Measures</h2> <p>Pipelines should reject or flag impossible or implausible measures rather than silently carry them forward. Depending on the dataset, checks may include:</p> <ul> <li>Negative billing amounts.</li> <li>Impossible ages.</li> <li>Unknown categorical values after harmonization.</li> <li>Extremely high length-of-stay values requiring review.</li> </ul> <p>The purpose is not to discard unusual data automatically. It is to make unusual values visible and auditable.</p> <h2 id="5-compare-runs-over-time">5. Compare Runs Over Time</h2> <p>A pipeline can pass row-level validation while still producing an unexpected change in aggregate outputs. Add run-level comparisons:</p> <ul> <li>Counts by year and source file.</li> <li>Event counts by demographic grouping.</li> <li>Missingness trends for key fields.</li> <li>Aggregate measures compared with prior runs.</li> </ul> <p>A sudden shift may reflect a real source-data update, but it should be reviewed before publication or dashboard release.</p> <h2 id="6-produce-a-validation-artifact">6. Produce A Validation Artifact</h2> <p>Validation should leave evidence. A useful pipeline output includes:</p> <ul> <li>Rules executed.</li> <li>Pass/fail result.</li> <li>Number of failing records.</li> <li>Run timestamp and input partitions.</li> <li>Links to quarantined or reviewable records where applicable.</li> </ul> <p>This turns quality checks into part of the data product rather than an informal step performed only during development.</p> <h2 id="closing-principle">Closing Principle</h2> <p>Reliable analytics begins before the statistical model or visualization. When the dataset grain, identifiers, temporal logic, and validation outputs are explicit, analysts can spend less time questioning pipeline behavior and more time interpreting results responsibly.</p>]]></content><author><name>Mahima Kaur</name></author><category term="data-engineering"/><category term="data-quality"/><category term="reproducibility"/><summary type="html"><![CDATA[A validation checklist for identifiers, time logic, duplication, measures, and cohort-ready data products.]]></summary></entry><entry><title type="html">Reliable Incremental Loads and Backfills for Time-Partitioned Data</title><link href="https://kaurmahima.github.io/blog/2026/reliable-incremental-loads-and-backfills/" rel="alternate" type="text/html" title="Reliable Incremental Loads and Backfills for Time-Partitioned Data"/><published>2026-05-08T00:00:00+00:00</published><updated>2026-05-08T00:00:00+00:00</updated><id>https://kaurmahima.github.io/blog/2026/reliable-incremental-loads-and-backfills</id><content type="html" xml:base="https://kaurmahima.github.io/blog/2026/reliable-incremental-loads-and-backfills/"><![CDATA[<p>Monthly data pipelines often begin as a script that downloads the latest file and appends results. That approach works until a file changes, an earlier month fails, or a transformation rule must be corrected across historical data.</p> <p>Incremental processing should be designed with backfills in mind from the start.</p> <h2 id="use-time-partitions-as-a-processing-boundary">Use Time Partitions As A Processing Boundary</h2> <p>When data arrives monthly, write outputs by year and month:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>data/bronze/year=2026/month=01/events.parquet
data/bronze/year=2026/month=02/events.parquet
</code></pre></div></div> <p>Each partition becomes an independently inspectable unit. This provides:</p> <ul> <li>Clear input-to-output lineage.</li> <li>Smaller rerun scope after a failure.</li> <li>Straightforward completeness checks.</li> <li>Efficient filtering during downstream queries.</li> </ul> <h2 id="make-idempotency-explicit">Make Idempotency Explicit</h2> <p>A safe pipeline should define what happens when a partition already exists. Common options include:</p> <ul> <li>Skip already completed partitions by default.</li> <li>Require an explicit <code class="language-plaintext highlighter-rouge">--force</code> or backfill flag to replace data.</li> <li>Record completion metadata separately from file existence.</li> </ul> <p>A CLI pattern for controlled reruns:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python ingest.py <span class="nt">--year</span> 2026 <span class="nt">--month</span> 4
python ingest.py <span class="nt">--year</span> 2026 <span class="nt">--month</span> 4 <span class="nt">--force</span>
</code></pre></div></div> <p>The first command supports routine operation. The second makes a deliberate replacement visible in logs and run metadata.</p> <h2 id="separate-ingestion-from-transformation">Separate Ingestion From Transformation</h2> <p>Source retrieval and analytical transformation fail for different reasons. Keep them separate:</p> <ol> <li>Ingest the raw or standardized source partition.</li> <li>Validate the stored output.</li> <li>Build modeled tables from valid partitions.</li> <li>Publish dashboards or aggregate outputs only after modeling succeeds.</li> </ol> <p>This separation allows a transformation model to be rebuilt without re-downloading data, and allows ingestion to recover independently when a source endpoint fails.</p> <h2 id="track-run-state-and-outputs">Track Run State And Outputs</h2> <p>At minimum, record:</p> <ul> <li>Pipeline name.</li> <li>Partition processed.</li> <li>Source or input reference.</li> <li>Output path.</li> <li>Completion time.</li> <li>Run status and failure detail.</li> </ul> <p>With run state, a scheduler can distinguish between a partition that was never processed, one that failed, and one that completed but must be replaced because logic changed.</p> <h2 id="schedule-routine-runs-parameterize-backfills">Schedule Routine Runs, Parameterize Backfills</h2> <p>Scheduled monthly workflows generally target the last completed month. Manual runs should accept historical parameters:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">year</span><span class="pi">:</span> <span class="m">2025</span>
<span class="na">month</span><span class="pi">:</span> <span class="m">11</span>
<span class="na">force</span><span class="pi">:</span> <span class="kc">true</span>
</code></pre></div></div> <p>This pattern supports predictable recurring loads while keeping historical correction workflows controlled and reviewable.</p> <h2 id="final-takeaway">Final Takeaway</h2> <p>Incremental loading is not only a performance technique. In research and analytical systems, it is a reproducibility design decision. Time partitions, idempotent reruns, explicit state, and parameterized backfills make it possible to explain exactly how a dataset was built and how it changed.</p>]]></content><author><name>Mahima Kaur</name></author><category term="data-engineering"/><category term="reproducibility"/><category term="research-software"/><summary type="html"><![CDATA[How to design monthly data workflows that rerun safely, expose lineage, and support historical corrections.]]></summary></entry><entry><title type="html">Presenting a reproducible health claims data pipeline at USRSE’25</title><link href="https://kaurmahima.github.io/blog/2025/usrse25-reproducible-health-claims-pipeline/" rel="alternate" type="text/html" title="Presenting a reproducible health claims data pipeline at USRSE’25"/><published>2025-10-06T00:00:00+00:00</published><updated>2025-10-06T00:00:00+00:00</updated><id>https://kaurmahima.github.io/blog/2025/usrse25-reproducible-health-claims-pipeline</id><content type="html" xml:base="https://kaurmahima.github.io/blog/2025/usrse25-reproducible-health-claims-pipeline/"><![CDATA[<div class="post-inline-media"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/USRSEConference2025_1029-480.webp 480w,/assets/img/USRSEConference2025_1029-800.webp 800w,/assets/img/USRSEConference2025_1029-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/USRSEConference2025_1029.jpg" class="post-inline-thumbnail rounded z-depth-1" width="100%" height="auto" alt="Mahima Kaur presenting at USRSE 2025 in Philadelphia" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">USRSE'25</figcaption> </figure> </div> <p>On October 6, 2025, I presented <strong>“A reproducible and scalable pipeline for processing administrative health claims data”</strong> at <a href="https://us-rse.org/usrse25/">USRSE’25</a>, the US Research Software Engineering Conference in Philadelphia. The conference theme, <em>Code, Practices, and People</em>, was a natural setting for work centered on scalable data engineering and reproducible research workflows.</p> <h2 id="motivation">Motivation</h2> <p>Administrative health claims data support important research on healthcare utilization, disease burden, and policy impact. They are also difficult to work with at scale: file formats and variables can change across years, coding conventions require careful harmonization, and repeated preprocessing creates duplicated effort across research teams.</p> <p>Our goal was to make that preprocessing more transparent and reusable by developing a pipeline that transforms raw CMS Medicare files into validated, analysis-ready datasets.</p> <h2 id="pipeline-overview">Pipeline overview</h2> <p>The presentation described a modular workflow with four main stages:</p> <ol> <li>Parse raw fixed-width Medicare files and metadata into columnar Parquet data.</li> <li>Harmonize year-to-year differences through declarative YAML transformation rules.</li> <li>Normalize the processed information into beneficiaries, enrollment, and admissions datasets.</li> <li>Generate materialized views for rapid cohort creation, longitudinal follow-up, and outcome-based analyses.</li> </ol> <p>The implementation uses <strong>Snakemake</strong> for workflow orchestration, <strong>Docker</strong> for portability, and <strong>DuckDB</strong> for efficient SQL queries over partitioned Parquet files. Together, these tools support a reproducible workflow without requiring a centralized database infrastructure.</p> <h2 id="why-this-work-matters">Why this work matters</h2> <p>For research teams working with complex administrative data, a reusable pipeline can reduce repeated cleaning, improve traceability, and make downstream analyses easier to reproduce. This approach also aligns with FAIR data principles by improving how derived data products and transformation steps are managed and reused.</p> <h2 id="collaborators-and-resources">Collaborators and resources</h2> <p>This work was completed with Shreya Nalluri, James C. Kitch, Tinashe M. Tapera, Jonathan Gilmour, Michelle Audirac, and Danielle Braun.</p> <ul> <li><a href="https://doi.org/10.5281/zenodo.17281686">Presentation record and slides on Zenodo</a></li> <li><a href="https://us-rse.org/usrse25/">USRSE’25 conference website</a></li> </ul> <p>Presenting this work at USRSE’25 provided an opportunity to share a practical data engineering approach with a community focused on reliable and sustainable research software.</p>]]></content><author><name>Mahima Kaur</name></author><category term="presentations"/><category term="data-engineering"/><category term="research-software"/><category term="reproducibility"/><category term="presentation"/><summary type="html"><![CDATA[A presentation on building a modular, scalable workflow for transforming Medicare administrative data into analysis-ready datasets.]]></summary></entry></feed>